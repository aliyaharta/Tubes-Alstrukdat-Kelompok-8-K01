#include <stdio.h>
#include "user.h"

void bonus(){
    char array[15];
    char temp[15];
    scanf("%s",array);

    for(int i=0; i<array; i++){
        if (array[i]=='T'){
            temp[i]='A';
        }
        else if(array[i]=='A'){
            temp[i]='U';
        }
        else if(array[i]=='C'){
            temp[i]='G';
        }
        else if(array[i]=='G'){
            temp[i]='C';
        }
        else if
    }
}